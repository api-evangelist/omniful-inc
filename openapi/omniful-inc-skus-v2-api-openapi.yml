openapi: 3.0.3
info:
  title: Omniful Integration APIs Background Jobs SKUs (V2) API
  version: v1
  description: Omniful unified supply chain and fulfillment platform integration APIs (Order Management, Warehouse/Inventory, Returns, Purchase Orders, Shipment/TMS, Webhooks). Derived faithfully from the provider-published Postman collection at docs.omniful.tech; paths, methods, parameters, and request examples are as published, response schemas are not enumerated in the source.
  contact:
    url: https://docs.omniful.tech
servers:
- url: https://prodapi.omniful.com
security:
- bearerAuth: []
tags:
- name: SKUs (V2)
paths:
  /sales-channel/public/v2/seller/master/skus:
    get:
      summary: Get Master Catalog SKUs
      operationId: getGetMasterCatalogSkus
      tags:
      - SKUs (V2)
      responses:
        '200':
          description: Successful response
  /sales-channel/public/v2/tenants/sellers/{seller_code}/skus:
    get:
      summary: Get SKUs
      operationId: getGetSkus
      tags:
      - SKUs (V2)
      parameters:
      - name: seller_code
        in: path
        required: true
        schema:
          type: string
      responses:
        '200':
          description: Successful response
components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
